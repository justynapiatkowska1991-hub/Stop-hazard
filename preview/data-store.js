const StopHazardStore=(()=>{const KEYS={events:"stopHazardEvents",domains:"stopHazardDomains",settings:"stopHazardSettings",stats:"stopHazardStats"};const seed=[{host:"bet365.com",blocked:true,age:"teraz",ts:Date.now()},{host:"example-bet.com",blocked:true,age:"2 min",ts:Date.now()-120000},{host:"youtube.com",blocked:false,age:"5 min",ts:Date.now()-300000}];const read=(k,f)=>{try{const v=JSON.parse(localStorage.getItem(k));return v??f}catch{return f}};const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));function events(){return read(KEYS.events,seed)}function addEvent(host,blocked,reason=""){const e=events();e.unshift({host:String(host).toLowerCase(),blocked,reason,ts:Date.now(),age:"teraz"});write(KEYS.events,e.slice(0,200));const s=stats();s.total++;if(blocked)s.blocked++;else s.allowed++;write(KEYS.stats,s);return e[0]}function domains(){return read(KEYS.domains,[])}function addDomain(host){const d=domains();if(!d.includes(host))d.push(host);write(KEYS.domains,d.slice(0,500));return d}function removeDomain(host){const d=domains().filter(x=>x!==host);write(KEYS.domains,d);return d}function settings(){return read(KEYS.settings,{enabled:true,strict:true,notifications:true})}function setSettings(p){const s={...settings(),...p};write(KEYS.settings,s);return s}function stats(){return read(KEYS.stats,{total:3,blocked:2,allowed:1})}function clearEvents(){write(KEYS.events,[]);write(KEYS.stats,{total:0,blocked:0,allowed:0})}function isBlocked(host){const h=String(host).toLowerCase().replace(/^www\\./,"");return domains().some(d=>h===d||h.endsWith("."+d))}return{events,addEvent,domains,addDomain,removeDomain,settings,setSettings,stats,clearEvents,isBlocked}})();